# The full vendor/gxemul and vendor/mame trees stay local — they're
# pristine upstream sources, best re-cloned than mirrored into the
# repo. We check in SOURCES.md telling readers where to get them.
vendor/gxemul/
vendor/mame/
# Build artifacts
build/
*.o
*.a
